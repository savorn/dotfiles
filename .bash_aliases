# emacs in terminal
alias emacs='emacs -nw'

# list files & directory
alias ls='ls -alh --color=auto'

# convert audio to mp3 using lame encoder
alias ffmpeg_audio='ffmpeg -c:v copy -c:a libmp3lame -q:a 4'
